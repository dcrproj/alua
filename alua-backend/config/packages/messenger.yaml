framework:
    messenger:
        failure_transport: failed

        transports:
            async:  'doctrine://default?queue_name=default'
            failed: 'doctrine://default?queue_name=failed'

        routing:
            'App\Message\RefreshDpeMessage': async
